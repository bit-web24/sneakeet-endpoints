<!DOCTYPE html>
<html>

<head>
    <title>Product Details Form</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100 flex items-center justify-center h-screen mt-32 mb-32">
    <div class="w-2/3 bg-gray-800 text-gray-200 p-8 shadow-md rounded-lg">
        <h1 class="text-2xl font-semibold mb-6 text-center">Edit Product</h1>
        <form action="/admin/products/<%= product._id %>?_method=PUT" method="POST">
            <label for="name" class="block font-semibold mb-2">Name:</label>
            <input type="text" id="name" name="name"
                class="w-full border rounded-lg px-3 py-2 mb-3 bg-gray-600 text-gray-200 focus:outline-none focus:border-blue-500"
                value="<%= product.name %>">

            <label for="brand" class="block font-semibold mb-2">Brand:</label>
            <input type="text" id="brand" name="brand"
                class="w-full border rounded-lg px-3 py-2 mb-3 bg-gray-600 text-gray-200 focus:outline-none focus:border-blue-500"
                value="<%= product.brand %>">

            <label for="price" class="block font-semibold mb-2">Price:</label>
            <input type="number" id="price" name="price"
                class="w-full border rounded-lg px-3 py-2 mb-3 bg-gray-600 text-gray-200 focus:outline-none focus:border-blue-500"
                value="<%= product.price %>">

            <label for="description" class="block font-semibold mb-2">Description:</label>
            <textarea id="description" name="description"
                class="w-full border rounded-lg px-3 py-2 mb-3 bg-gray-600 text-gray-200 focus:outline-none focus:border-blue-500"><%= product.description %></textarea>

            <label for="category" class="block font-semibold mb-2">Category:</label>
            <input type="text" id="category" name="category"
                class="w-full border rounded-lg px-3 py-2 mb-3 bg-gray-600 text-gray-200 focus:outline-none focus:border-blue-500"
                value="<%= product.category.join(', ') %>">

            <label for="sizes" class="block font-semibold mb-2">Sizes (comma-separated):</label>
            <input type="text" id="sizes" name="sizes"
                class="w-full border rounded-lg px-3 py-2 mb-3 bg-gray-600 text-gray-200 focus:outline-none focus:border-blue-500"
                value="<%= product.sizes.join(', ') %>">

            <label for="colors" class="block font-semibold mb-2">Colors (comma-separated):</label>
            <input type="text" id="colors" name="colors"
                class="w-full border rounded-lg px-3 py-2 mb-3 bg-gray-600 text-gray-200 focus:outline-none focus:border-blue-500"
                value="<%= product.colors.join(', ') %>">

            <label for="imgs" class="block font-semibold mb-2">Images:</label>
            <input type="file" id="imgs" name="imgs" multiple
                class="w-full border rounded-lg px-3 py-2 mb-3 bg-gray-600 text-gray-200 focus:outline-none focus:border-blue-500">

            <label for="availability" class="inline-flex items-center mb-3">
                <input type="checkbox" id="availability" name="availability" class="mr-2 leading-tight" <% if
                    (product.availability) { %>checked<% } %>>
                    <span class="text-sm">Available</span>
            </label>

            <button type="submit" class="w-full bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded-lg">
                Update Product
            </button>
        </form>
    </div>
</body>

</html>